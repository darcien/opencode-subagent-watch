{
  "name": "opencode-subagent-watch",
  "version": "0.1.0",
  "description": "OpenCode TUI plugin for monitoring subagent activity",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    "./tui": {
      "import": "./dist/tui.js"
    }
  },
  "scripts": {
    "build": "bun scripts/build.ts",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "lint": "oxlint --deny-warnings .",
    "lint:fix": "oxlint --fix .",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "precommit:lint": "bun run lint",
    "precommit:typecheck": "bun run typecheck",
    "precommit:test": "bun test",
    "precommit": "bun run fmt && bun run --parallel --no-exit-on-error 'precommit:*'",
    "check": "bun run precommit && bun run build && bun scripts/smoke-package.ts"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "1.18.9",
    "@opencode-ai/sdk": "1.18.9",
    "@opentui/core": "0.4.5",
    "@opentui/keymap": "0.4.5",
    "@opentui/solid": "0.4.5",
    "@types/bun": "1.3.14",
    "oxfmt": "0.61.0",
    "oxlint": "1.60.0",
    "solid-js": "1.9.12",
    "typescript": "5.9.3"
  },
  "peerDependencies": {
    "@opentui/solid": ">=0.4.5",
    "solid-js": "1.9.12"
  }
}
